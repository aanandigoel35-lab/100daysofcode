#include <stdio.h>
int main() {
    FILE *f;
    char name[50];
    int age;

    f = fopen("info.txt","w");

    scanf("%s %d", name, &age);
    fprintf(f,"Name:%s\nAge:%d",name,age);
    fclose(f);

    printf("Saved Successfully");
}
